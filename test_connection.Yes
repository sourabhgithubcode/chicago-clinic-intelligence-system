from sqlalchemy import create_engine

  url = "mssql+pytds://CloudSA6ec5ad8f:Sagardagar%4021212@srodagi.database.windows.net:1433/clinic-intelligence-db"

  print("Testing connection...")
  try:
      engine = create_engine(url, connect_args={'timeout': 10, 'encrypt': True})
      conn = engine.connect()
      print("✅ SUCCESS!")
      conn.close()
  except Exception as e:

