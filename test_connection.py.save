
url = "mssql+pytds://CloudSA6ec5ad8f:Sagardagar@21212@srodagi.database.windows.net:1433/clinic-intelligence-db?encryption=required"

print("Testing connection...")
try:
    engine = create_engine(url, connect_args={'timeout': 10})
    conn = engine.connect()
    print("✅ SUCCESS!")
    conn.close()
except Exception as e:
    print(f"❌ Error: {e}")
